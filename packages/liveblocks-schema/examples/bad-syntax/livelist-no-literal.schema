type Foo { bad: LiveList<"literal"> }
                         ^ Expected "LiveList", "LiveMap", "LiveObject", "boolean", "number", "string", "{", or <type name> but "\"" found
                         # This should be supported when we add string literal support!
